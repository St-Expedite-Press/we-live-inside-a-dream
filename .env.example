# Copy to `.env` and fill in values.
# Never commit `.env`.

# GitHub repo URL, e.g. https://github.com/<owner>/<repo>.git
GITHUB_URL=

# Personal Access Token (classic or fine-grained). Keep private.
GITHUB_PAT=

# OpenAI API key for orchestration enrichment/classification (Responses API).
OPENAI_API_KEY=

# Optional default model for orchestration command.
OPENAI_MODEL=gpt-4.1-mini
