# Runbook — Image Restoration Pipeline

## Run ID policy

run_id format: outputs root: `outputs/<run_id>/`.
## Steps

1. Prompt:
Inputs: Outputs: Stop condition: (Order preserved.)
2. Prompt:
Inputs: Outputs: Stop condition: (Order preserved.)
## Final artifact map

`outputs/<run_id>/effective_config.yaml`. `outputs/<run_id>/inputs_manifest.json`. `outputs/<run_id>/restored_bw/`. `outputs/<run_id>/colorized/` (if applicable). `outputs/<run_id>/reports/`. (Order preserved.)
